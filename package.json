{
  "name": "materidex",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "audit:materials": "node scripts/audit-materials.js",
    "test:smoke": "node scripts/check-app-smoke.js",
    "validate:structures": "node scripts/validate-structures.js",
    "test": "npm run test:smoke && node scripts/check-render-models.js && node scripts/check-first-mission.js && node scripts/check-mission-science.js && node scripts/check-lab-viewer-layout.js && node scripts/check-vault-lifecycle.js && node scripts/check-codex-lifecycle.js",
    "build": "npm run audit:materials && npm run validate:structures && npm test && node scripts/check-build.js"
  }
}
